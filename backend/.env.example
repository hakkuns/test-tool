# Backend Server Configuration
PORT=3001

# Database Configuration
DATABASE_URL=postgresql://username:password@host:5432/database

# Environment
NODE_ENV=production

# API Proxy Configuration (Optional)
# If targeting another dev container, specify the container name
# Example: TARGET_API_CONTAINER=my-spring-api
# If not specified, DOCKER_HOST_IP or host.docker.internal will be used
TARGET_API_CONTAINER=

# Docker Host IP Address
# If host.docker.internal is not available, specify the gateway IP
# Example: DOCKER_HOST_IP=172.19.0.1
DOCKER_HOST_IP=172.19.0.1

# Database Encryption Key (for pgcrypto)
# Used by pgp_sym_encrypt/pgp_sym_decrypt functions
# Use a strong key in production
ENCRYPTION_KEY=your-secret-encryption-key-here
