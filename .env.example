# Docker Compose Configuration
# This file is used for variable substitution in docker-compose.yml

# External Docker Network (Required)
# Specify the name of the external Docker network to connect to
# This network must exist before running docker-compose up
# Example: EXTERNAL_NETWORK=my_shared_network
EXTERNAL_NETWORK=network_name_here
